# The real secret lives outside this repo (and thus outside bin/claude-sandbox's
# --mount-dir bind mount) so a sandboxed agent session can never read it off disk -
# see docs/microsandbox-agent-sessions.md's "One-time host setup".
source_env ~/.secrets/datatable-bundle/github-token.envrc
